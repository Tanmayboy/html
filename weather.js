const movingText = document.getElementById('moving-text');

// Change the text
movingText.innerHTML = 'New text';

// Change the duration of the animation
movingText.style.animationDuration = '10s';